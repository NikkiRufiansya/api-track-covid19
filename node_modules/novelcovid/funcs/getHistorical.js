const fetch = require('node-fetch');

module.exports = async function(params) {

    if (params.country) {
        try { return await fetch(`https://corona.lmao.ninja/v2/historical/${params.country}`).then(r=>r.json()); } catch (e) { throw new Error("Country could not be found.") };
    }
}